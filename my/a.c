#include <stdio.h>

int main(int argc, char *argv) {
    // printf("Hello world!\n");
    // fputs("Hello world!\n", stdout);

    // puts("Hello world!");

    // char buf[1024];

    // fgets(buf, sizeof buf, stdin);

    // printf("%s", buf);
    // puts(buf);

    int n;

    scanf("%d", &n);

    printf("%d\n", n);
}
